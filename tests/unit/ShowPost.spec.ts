import { mount } from '@vue/test-utils'
import ShowPost from '../../src/components/ShowPost.vue'

describe('ShowPost', () => {
  it('does not show edit button when not authenticated', () => {
  })

  it('does not show edit button when not authorized', () => {
  })

  it('shows edit button when authorized', () => {
  })
})